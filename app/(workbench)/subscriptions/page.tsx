import { Subscriptions } from '@/ui/pages/Subscriptions';

export const metadata = { title: 'Subscriptions — Personal finance' };

export default function SubscriptionsPage() {
  return <Subscriptions />;
}
